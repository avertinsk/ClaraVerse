# ============================================================
# ClaraVerse Production Stack — адаптировано под инфраструктуру:
#   LAN: 10.10.0.1/20
#   GPU: NVIDIA RTX 2070 Super (8GB VRAM)
#   Ollama: работает на хосте, порт 11434
# ============================================================

services:
  # ============================
  # Базы данных (bridge-сеть, существующие контейнеры)
  # ============================
  mongodb:
    image: mongo:7
    container_name: claraverse-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=claraverse
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      start_period: 30s
      retries: 3
    networks:
      - claraverse-network

  redis:
    image: redis:7-alpine
    container_name: claraverse-redis
    command: redis-server --appendonly yes --maxmemory 100mb --maxmemory-policy allkeys-lru --port 6380
    ports:
      - "6380:6380"
    volumes:
      - redis-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "6380", "ping"]
      interval: 10s
      timeout: 5s
      start_period: 5s
      retries: 5
    networks:
      - claraverse-network

  mysql:
    image: mysql:8.0
    container_name: claraverse-mysql
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql-data-new:/var/lib/mysql
      - ./backend/migrations:/docker-entrypoint-initdb.d:ro
    environment:
      - MYSQL_ROOT_PASSWORD=Testeriks~235
      - MYSQL_DATABASE=claraverse
      - MYSQL_USER=claraverse_user
      - MYSQL_PASSWORD=Testeriks~235
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-claraverse_root_2024}"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 5
    networks:
      - claraverse-network

  # ============================
  # Docling — document recognition (OCR, PDF structure, Markdown output)
  # ============================
  docling:
    image: quay.io/docling-project/docling-serve-cpu:latest
    container_name: claraverse-docling
    ports:
      - "5001:5001"
    environment:
      - DOCLING_SERVE_ENABLE_UI=0
      - DOCLING_SERVE_WORKERS=1
      - DOCLING_SERVE_TIMEOUT=300
    restart: unless-stopped
    networks:
      - claraverse-network
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5001/health')"]
      interval: 30s
      timeout: 10s
      start_period: 180s
      retries: 6

  searxng:
    image: searxng/searxng:latest
    container_name: claraverse-searxng
    ports:
      - "8080:8080"
    volumes:
      - searxng-config:/etc/searxng
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        if [ ! -f /etc/searxng/settings.yml ]; then
          cat > /etc/searxng/settings.yml <<'SETTINGS'
        use_default_settings: true
        server:
          limiter: false
          secret_key: "claraverse-searxng-$(head -c 16 /dev/urandom | od -A n -t x1 | tr -d ' \n')"
          image_proxy: true
        search:
          formats:
            - html
            - json
          autocomplete: "duckduckgo"
        SETTINGS
        fi
        exec /usr/local/searxng/entrypoint.sh
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/"]
      interval: 30s
      timeout: 3s
      start_period: 15s
      retries: 3
    networks:
      - claraverse-network

  # ============================
  # Qdrant — векторная БД для семантического поиска (RAG)
  # ============================
  qdrant:
    image: qdrant/qdrant:latest
    container_name: claraverse-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    restart: unless-stopped
    networks:
      - claraverse-network
    healthcheck:
      test: ["CMD", "pidof", "qdrant"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

  # ============================
  # E2B Code Executor — локальное исполнение Python кода
  # ============================
  e2b-service:
    build:
      context: ./backend/e2b-service
    container_name: claraverse-e2b
    ports:
      - "8001:8001"
    restart: unless-stopped
    networks:
      - claraverse-network
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

  # ============================
  # ClaraVerse (сборка из исходников + host.docker.internal)
  # ============================
  claraverse:
    build:
      context: .
      dockerfile: Dockerfile
    image: claraverse:${CLARAVERSE_TAG:-latest}
    container_name: claraverse
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - claraverse-data:/app/data
      - claraverse-uploads:/app/uploads
      - /srv/smb:/data/smb:ro
    environment:
      - ENVIRONMENT=production
      - SERVE_FRONTEND=true
      - PORT=3000
      - DATABASE_URL=mysql://claraverse_user:Testeriks~235@mysql:3306/claraverse?parseTime=true
      - MONGODB_URI=mongodb://mongodb:27017/claraverse
      - REDIS_URL=redis://redis:6380
      - SEARXNG_URL=http://searxng:8080
      - SEARXNG_URLS=http://searxng:8080
      - E2B_SERVICE_URL=http://e2b-service:8001
      - DOCLING_URL=http://docling:5001
      - QDRANT_URL=http://qdrant:6333
      - EMBEDDING_MODEL=nomic-embed-text:v1.5
      - FRONTEND_URL=http://10.10.0.1:3000
      - BACKEND_URL=http://10.10.0.1:3000
      - ALLOWED_ORIGINS=*
      - JWT_SECRET=${JWT_SECRET}
      - ENCRYPTION_MASTER_KEY=${ENCRYPTION_MASTER_KEY}
      - OLLAMA_BASE_URL=http://host.docker.internal:11434
      - COMPOSIO_API_KEY=${COMPOSIO_API_KEY}
      - E2B_API_KEY=${E2B_API_KEY}
      - UPLOAD_DIR=/app/uploads
      - PROVIDERS_FILE=/app/data/providers.json
      - DOCKER=true
    depends_on:
      mongodb:
        condition: service_healthy
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
      searxng:
        condition: service_healthy
      e2b-service:
        condition: service_started
      qdrant:
        condition: service_started
      docling:
        condition: service_started
    restart: unless-stopped
    networks:
      - claraverse-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

volumes:
  claraverse-data:
    driver: local
  claraverse-uploads:
    driver: local
  mongodb-data:
    driver: local
  mysql-data-new:
    driver: local
  redis-data:
    driver: local
  searxng-config:
    driver: local
  qdrant-data:
    driver: local

networks:
  claraverse-network:
    driver: bridge
